import Grid from "@mui/material/Grid";

export default function Landing() {
    return (
        <Grid container sx={{ display: "flex", border: "1px solid black", justifyContent: "center", alignItems: "center", height: "100vh" }}>
            <h1>Welcome dumbass!</h1>
        </Grid>
    );
}